import pkg from 'pg';
const { Pool } = pkg;

const pool = new Pool({
  user: 'yourusername',
  host: 'localhost',
  database: 'yourdatabasename',
  password: 'yourpassword',
  port: yourport,
});

export default pool;
